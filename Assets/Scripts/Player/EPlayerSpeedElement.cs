using Utilities.StackableElement.SpeedHandler;

namespace Utilities.StackableElement
{
    /// <summary>
    /// A enum for all the the possible <see cref="SpeedElement"/> of player.
    /// </summary>
    public enum EPlayerSpeedElement
    {
        WalkSpeed = 0,
        RunSpeed = 1,
        SilentWalk = 2
    }
}